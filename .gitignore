# OS files (two devs, mixed OS)
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
Desktop.ini

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# nyc test coverage
.nyc_output

# Grunt intermediate storage (https://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release
build/

.next/

# Dependency directories
node_modules/
jspm_packages/

# Snowpack dependency directory (https://snowpack.dev/)
web_modules/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variable files
.env
.env.*
!.env.example

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next
out

# Nuxt.js build / generate output
.nuxt
dist
.output

# Gatsby files
.cache/
# Comment in the public line in if your project uses Gatsby and not Next.js
# https://nextjs.org/blog/next-9-1#public-directory-support
# public

# vuepress build output
.vuepress/dist

# vuepress v2.x temp directory
.temp

# Sveltekit cache directory
.svelte-kit/

# vitepress build output
**/.vitepress/dist

# vitepress cache directory
**/.vitepress/cache

# Docusaurus cache and generated files
.docusaurus

# Serverless directories
.serverless/

# FuseBox cache
.fusebox/

# DynamoDB Local files
.dynamodb/

# Firebase cache directory
.firebase/

# TernJS port file
.tern-port

# Stores VSCode versions used for testing VSCode extensions
.vscode-test

# pnpm
.pnpm-store
package-lock.json

# yarn v3
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Vite files
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
.vite/
.idea/

# Claude Code local settings
.claude/

# Prisma generated client (regenerated on install)
**/src/generated/

# Husky
.husky/_/

# Storybook
frontend/storybook-static/

# Lighthouse
.lighthouseci/

# Agents
.agents/
.antigravityignore
GEMINI.md

# SVG
*.svg

# Graphify
graphify-out/

# Nx / Turbo / Workspace Build
.nx/
.turbo/
apps/*/dist/
apps/*/coverage/

# CodeQL
codeql-alerts.json

# Benchmarks & Load Testing
benchmarks/reports/
benchmarks/k6/tokens.json

# Clinic.js profiling output
*.clinic-*/
*.clinic-*
.clinic/

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log

# React Native / mobile (apps/mobile roadmap) — NEVER commit signing
# credentials or push-notification keys in the open-source repo. When you need
# a template, commit NAME.example (e.g. google-services.json.example) instead.
*.keystore
*.jks
*.p8
*.p12
*.pem
*.key
local.properties
.gradle/
**/android/app/build/
**/ios/Pods/
**/ios/build/
xcuserdata/
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
.expo/
.expo-shared/
google-services.json
GoogleService-Info.plist

# napi-rs local prebuilds (generated .node binaries, never commit)
packages/native/prebuilds/

# Node heap snapshots (local profiling, e.g. backend/heapdumps/)
heapdumps/
*.heapsnapshot

# Vercel (frontend hosting — local deploy state, no secrets in repo)
.vercel/

# Rust build artifacts (rebuilt in CI / locally via cargo).
# NOTE: Cargo.lock is intentionally COMMITTED (not ignored): this crate ships
# compiled .node prebuilds and two devs build it locally, so locked deps give
# reproducible binaries. (Upstream "don't commit for libs" advice only applies
# to crates.io libraries with downstream consumers — we have none.)
packages/native/rust/target/
packages/text-pipeline/rust/target/
packages/blinded-crypto/rust/target/
packages/feed-score/rust/target/
