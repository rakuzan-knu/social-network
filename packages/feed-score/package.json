{
  "name": "@social-network/feed-score",
  "version": "1.0.0",
  "description": "Portable recommendation scoring v1: proximity + mutuals + popularity (+ recency decay + interest affinity). Pure math, zero dependencies, no Node APIs — Node, browsers, React Native.",
  "license": "AGPL-3.0-only",
  "private": true,
  "main": "./dist/index.js",
  "module": "./dist/esm/index.js",
  "types": "./dist/index.d.ts",
  "sideEffects": false,
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/esm/index.js",
      "require": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.esm.json && node -e \"require('fs').writeFileSync('dist/esm/package.json','{\\\"type\\\":\\\"module\\\"}')\"",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "npm run build && node --test tests/feed-score.test.cjs",
    "bench": "npm run build && node benches/feed-score.bench.cjs"
  },
  "dependencies": {
    "@social-network/text-pipeline": "workspace:*"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
